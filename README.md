Create a schema called "wapfinal" in MySql
    Connection string is inside /src/main/resources/META-INF/persistence.xml

Copy all the files inside /tomcat/lib to your tomcat/lib folder

Do not use Java 9
